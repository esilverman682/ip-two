/// <reference types="react" />
import { MonitorParams } from 'tweakpane/dist/types/api/types';
import { Schema, Separator, Folder, Button, Monitor } from './types';
export declare function makeSeparator(): Record<string, Separator>;
export declare function makeFolder<T extends Schema, P extends string>(title: P, schema: T, expanded?: boolean): Record<P, Folder<T>>;
export declare const makeDirectory: typeof makeFolder;
export declare function makeButton(title: string, onClick: () => void): Record<string, Button>;
export declare function makeMonitor(title: string, ref: any | React.Ref<any> | (() => any), settings: MonitorParams): Record<string, Monitor>;
